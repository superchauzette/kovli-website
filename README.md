# KØVLI one-page site

This is a static, responsive one-page artist website for KØVLI.

Edit the music content in `content.js`. Track order, descriptions, Spotify embeds, lyrics text, feature credits, images, and visual fragments all live there, so layout code does not need to change.

Open `index.html` directly in a browser, or serve the folder locally for the closest production behavior.
